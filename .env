# ============================================================================
# PENGUIN STATUS CONFIGURATION
# ============================================================================

# ============================================================================
# DATABASE SETTINGS
# ============================================================================
# SQLite database file location (automatically created if it doesn't exist)
# Default: ./database/uptime_monitor.db

# ============================================================================
# SERVER SETTINGS
# ============================================================================
# Port number for the web server
PORT=3001

# Host to bind the server to (0.0.0.0 for production, localhost for development)
HOST=0.0.0.0

# Domain URL for the application (used in console messages and redirects)
DOMAIN=http://localhost:3001

# CORS origin for Socket.IO (set to your domain for production)
CORS_ORIGIN=*

# ============================================================================
# SECURITY SETTINGS
# ============================================================================
# JWT Secret Key for authentication (CHANGE THIS IN PRODUCTION!)
# Generate a new secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================================================
# DISCORD NOTIFICATION SETTINGS (Optional)
# ============================================================================
# Discord Webhook for sending monitor alerts
# How to get webhook URL:
# 1. Go to your Discord channel settings
# 2. Click "Integrations" → "Webhooks" → "New Webhook"
# 3. Copy the webhook URL and paste it below
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Discord Channel ID where notifications will be sent
# Right-click on your Discord channel → "Copy Channel ID"
DISCORD_CHANNEL_ID=YOUR_DISCORD_CHANNEL_ID